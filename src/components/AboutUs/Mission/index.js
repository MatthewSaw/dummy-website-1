import React from 'react';
import { MissionContainer, MissionTitle, MissionDesc } from './MissionElements';

const Mission = () => {
    return (
        <MissionContainer>
            <MissionTitle>Our Mission</MissionTitle>
            <MissionDesc>Rewire the banking system so that we can positively change the financial path for every hard-working person.</MissionDesc>
        </MissionContainer>
    )
}

export default Mission;
